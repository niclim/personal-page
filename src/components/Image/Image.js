import React from 'react'
// eslint-disable-next-line
import { _Image } from './Image.style'

const Image = props => {
  return (
    // eslint-disable-next-line
    <_Image {...props} />
  )
}

export default Image
